name: biblioteca_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  firebase_core
  firebase_auth
  cloud_firestore
  firebase_storage
  cached_network_image
  flutter_rating_bar

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
